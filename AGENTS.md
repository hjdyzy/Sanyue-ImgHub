# 协作约定
- 始终使用中文回答。
- 修改前先确认需求，避免臆测或随意扩展范围。
- 前端为 Vue 3 + Vue Router + Vuex + Element Plus，保持现有组件与样式体系。
- 本仓库构建产物位于 `dist/`，需同步覆盖到 `CloudFlare-ImgBed` 的 `/css`、`/js`、`/img`、`index.html`、`logo*.png`。
- 开发使用 `npm run serve`，发布使用 `npm run build`。
